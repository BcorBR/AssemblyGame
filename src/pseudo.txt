player.x = playercoord mod 40
player.y = playercoord div 40
mob.x = mobcoord mod 40
mob.y = mobcoord div 40

if player.y >= mob.y:
	ydif = player.y - mob.y
else: 
	ydif = mob.y - player.y

if player.x > mob.x:
	xdif = player.x - mob.x
else:
	xdif = mob.x - player.x
	
if xdif >= ydif:
amount = xdif + ydif

